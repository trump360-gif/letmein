---
phase: 02-hospital-auth-dashboard
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - cms/apps/admin/src/middleware.ts
  - cms/apps/admin/src/lib/session.ts
  - cms/apps/admin/src/app/(hospital)/hospital-login/page.tsx
  - cms/apps/admin/src/app/(hospital)/layout.tsx
  - cms/apps/admin/src/app/(hospital)/(hospital-dashboard)/layout.tsx
  - cms/apps/admin/src/app/(hospital)/(hospital-dashboard)/page.tsx
  - cms/apps/admin/src/app/api/v1/hospital/auth/signin/route.ts
  - cms/apps/admin/src/app/api/v1/hospital/auth/signout/route.ts
  - cms/apps/admin/src/app/api/v1/hospital/dashboard/route.ts
  - cms/apps/admin/src/views/hospital/auth/HospitalLoginPage.tsx
  - cms/apps/admin/src/views/hospital/dashboard/HospitalDashboardView.tsx
  - cms/apps/admin/src/widgets/sidebar/HospitalSidebar.tsx
  - cms/packages/types/src/letmein.ts
autonomous: true
requirements:
  - HAUTH-01
  - HAUTH-02
  - HAUTH-03
  - HDASH-01
  - HDASH-02
  - HDASH-03

must_haves:
  truths:
    - "병원 계정이 /hospital-login에서 로그인하면 hospital 전용 사이드바가 표시된다"
    - "어드민 계정은 기존 어드민 사이드바를, 병원 계정은 병원 사이드바를 각각 표시한다"
    - "병원 계정이 /coordinator, /hospitals, /members 등 어드민 전용 라우트에 접근하면 /hospital/dashboard로 리다이렉트된다"
    - "병원 대시보드에서 신규 매칭 수, 활성 채팅 수, 평균 응답률, 평점 4개 통계를 확인할 수 있다"
    - "대시보드에서 최근 상담 요청 3건 미리보기를 확인할 수 있다"
    - "대시보드에서 최근 리뷰 3건 미리보기를 확인할 수 있다"
  artifacts:
    - path: "cms/apps/admin/src/app/(hospital)/hospital-login/page.tsx"
      provides: "병원 전용 로그인 페이지 라우트"
    - path: "cms/apps/admin/src/app/api/v1/hospital/auth/signin/route.ts"
      provides: "병원 로그인 API — User 테이블에서 role=hospital 계정 인증"
    - path: "cms/apps/admin/src/views/hospital/auth/HospitalLoginPage.tsx"
      provides: "병원 로그인 폼 UI (use client)"
    - path: "cms/apps/admin/src/widgets/sidebar/HospitalSidebar.tsx"
      provides: "병원 전용 사이드바"
    - path: "cms/apps/admin/src/app/(hospital)/(hospital-dashboard)/layout.tsx"
      provides: "병원 대시보드 레이아웃 (HospitalSidebar 포함)"
    - path: "cms/apps/admin/src/app/api/v1/hospital/dashboard/route.ts"
      provides: "대시보드 통계 API"
    - path: "cms/apps/admin/src/views/hospital/dashboard/HospitalDashboardView.tsx"
      provides: "대시보드 UI — 통계 카드 4개 + 최근 상담 3건 + 최근 리뷰 3건"
    - path: "cms/apps/admin/src/middleware.ts"
      provides: "role-based 라우트 보호"
    - path: "cms/apps/admin/src/lib/session.ts"
      provides: "getSessionRole() 헬퍼 추가"
  key_links:
    - from: "cms/apps/admin/src/middleware.ts"
      to: "/hospital/(hospital-dashboard)/*"
      via: "JWT payload role=hospital 검증 후 어드민 라우트 차단"
      pattern: "payload\\.role.*hospital"
    - from: "cms/apps/admin/src/views/hospital/auth/HospitalLoginPage.tsx"
      to: "/api/v1/hospital/auth/signin"
      via: "fetch POST"
      pattern: "hospital/auth/signin"
    - from: "cms/apps/admin/src/app/api/v1/hospital/dashboard/route.ts"
      to: "prisma.coordinatorMatch + prisma.chatRoom + prisma.$queryRaw(reviews)"
      via: "Prisma direct queries in Route Handler"
      pattern: "prisma\\.(coordinatorMatch|chatRoom|\\$queryRaw)"
---

<objective>
병원 계정이 /hospital-login으로 로그인하고, role에 따라 완전히 분리된 사이드바/레이아웃으로 진입하며, 어드민 전용 라우트에서 자동 차단되고, 병원 전용 대시보드(통계 4개 + 상담 3건 + 리뷰 3건)를 볼 수 있다.

Purpose: 병원이 CMS에서 비즈니스를 운영하기 위한 인증 관문 + 첫 화면 완성
Output:
- /hospital-login 로그인 페이지
- /hospital/dashboard 대시보드 페이지
- 역할 분기 미들웨어 (admin vs hospital 라우트 완전 분리)
- 병원 전용 사이드바
- 대시보드 통계 API + UI
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-cms-critical/01-PLAN-1-SUMMARY.md

<interfaces>
<!-- 기존 코드에서 추출한 핵심 계약. executor가 직접 사용한다. -->

From cms/apps/admin/src/lib/session.ts:
```typescript
// 현재 구현 — 확장 필요
export async function getSessionAdminId(): Promise<bigint | null>
// JWT cookie 'admin_token'의 sub claim을 BigInt로 반환

// 추가 필요
export async function getSessionRole(): Promise<'admin' | 'hospital' | null>
// JWT payload의 role claim 반환
export async function getSessionHospitalId(): Promise<bigint | null>
// role=hospital 세션에서 hospitalId 반환 (payload.hospitalId)
```

From cms/apps/admin/src/middleware.ts:
```typescript
// 현재: 토큰 존재 여부만 확인, /login과 /api 통과
// 변경 필요: role 분기 추가
// - role=admin: 어드민 라우트 통과, /hospital/* → /
// - role=hospital: /hospital/* 통과, 그 외(어드민) 라우트 → /hospital/dashboard
// - /hospital-login, /login, /api는 항상 통과
```

From cms/apps/admin/src/app/api/v1/admin/auth/signin/route.ts:
```typescript
// 어드민 로그인 패턴 (병원 로그인에서 그대로 따름)
// JWT payload: { sub, email, name, role, hospitalId? }
// cookie: 'admin_token', httpOnly, sameSite: lax, maxAge: 7d
// signout: DELETE cookie 'admin_token'
```

From cms/apps/admin/src/widgets/sidebar/index.tsx:
```typescript
// 현재 Sidebar: static navigation 배열, useAuthStore, useUIStore
// 병원 사이드바는 별도 파일로 분리:
// cms/apps/admin/src/widgets/sidebar/HospitalSidebar.tsx
// - 병원 전용 메뉴만 포함
// - 로그아웃은 /api/v1/hospital/auth/signout POST 후 /hospital-login으로 이동
```

From cms/packages/db/prisma/schema.prisma (relevant models):
```prisma
model User {
  id       BigInt  @id @default(autoincrement())
  role     String? @default("user") @db.VarChar(20)
  // 병원 계정: role = 'hospital'
}

model Hospital {
  id     BigInt @id @default(autoincrement())
  userId BigInt @unique @map("user_id")
  // hospital 계정의 hospitalId = hospitals.id WHERE user_id = user.id
  avgRating   Decimal? // avg_rating 컬럼 (reviews 마이그레이션에서 추가됨)
  reviewCount Int?     // review_count 컬럼
}

model CoordinatorMatch {
  id         BigInt   @id @default(autoincrement())
  hospitalId BigInt   @map("hospital_id")
  status     String?  @default("matched") @db.VarChar(20)
  createdAt  DateTime @map("created_at")
}

model ChatRoom {
  id         BigInt  @id @default(autoincrement())
  hospitalId BigInt  @map("hospital_id")
  status     String? @default("active") @db.VarChar(20)
}

// consultation_responses (이 모델명 그대로 사용)
model consultation_responses {
  id          BigInt   @id @default(autoincrement())
  request_id  BigInt
  hospital_id BigInt
  status      String?  @default("sent") @db.VarChar(20)
  created_at  DateTime @default(now())
}

// reviews 테이블은 Prisma 스키마에 없음 → prisma.$queryRaw 사용
// SELECT id, rating, content, created_at FROM reviews
// WHERE hospital_id = $1 AND status = 'active'
// ORDER BY created_at DESC LIMIT 3
```

From cms/packages/types/src/admin.ts:
```typescript
// AdminUser.role은 AdminRoleType = 'super_admin' | 'admin' | 'editor' | 'viewer'
// 병원 로그인 응답은 별도 타입 사용 — AdminUser를 재사용하지 않음
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 병원 로그인 API + 세션 헬퍼 확장 (HAUTH-01)</name>
  <files>
    cms/apps/admin/src/app/api/v1/hospital/auth/signin/route.ts,
    cms/apps/admin/src/app/api/v1/hospital/auth/signout/route.ts,
    cms/apps/admin/src/lib/session.ts,
    cms/packages/types/src/letmein.ts
  </files>
  <action>
**1. cms/apps/admin/src/app/api/v1/hospital/auth/signin/route.ts 생성**

POST handler:
- `{ email, password }` 받음
- `prisma.user.findFirst({ where: { nickname: email } })` 가 아닌 — User 테이블에는 email 컬럼이 없다. 병원 계정 조회 방식:
  - Go 서버의 User 테이블을 확인: `users` 테이블에 kakao_id, nickname 등은 있지만 email은 없음
  - 병원 계정은 별도 인증이 필요 — AdminCredential처럼 hospital_credentials 테이블을 만드는 것이 최선이나 이번 Plan에서는 AdminCredential 패턴을 재사용해 `admin_credentials` 테이블에 `role: 'hospital'` 컬럼을 추가하는 대신, 가장 단순한 방법을 택한다:
  - **AdminCredential 모델에 role 컬럼 추가** — `prisma db push` 불가이므로 `prisma.$executeRaw` 로 `ALTER TABLE admin_credentials ADD COLUMN IF NOT EXISTS role VARCHAR(20) DEFAULT 'admin'` 실행 후 `prisma generate` 실행
  - 단, Prisma schema에 role 필드 추가도 필요: `schema.prisma`의 `AdminCredential` 모델에 `role String @default("admin") @db.VarChar(20)` 추가
  - 로그인 시: `prisma.adminCredential.findFirst({ where: { email, isActive: true } })` → bcrypt.compare → role이 'hospital'인 경우에만 병원 로그인 성공
  - role이 'admin'이면 401 반환 ("병원 계정이 아닙니다")
  - 로그인 성공 시: `prisma.hospital.findUnique({ where: { userId: credential.id } })` 로 hospitalId 조회
  - JWT payload: `{ sub: credential.id.toString(), email, name, role: 'hospital', hospitalId: hospital.id.toString() }`
  - cookie 이름: `admin_token` (기존 미들웨어가 이 쿠키를 읽으므로 동일하게 사용)
  - 응답: `{ success: true, data: { user: { id, email, name, role: 'hospital', hospitalId } } }`

**2. cms/apps/admin/src/app/api/v1/hospital/auth/signout/route.ts 생성**

POST handler:
- `admin_token` 쿠키 삭제
- `{ success: true }` 반환

**3. cms/packages/db/prisma/schema.prisma 수정**

AdminCredential 모델에 role 필드 추가:
```prisma
model AdminCredential {
  ...
  role         String    @default("admin") @db.VarChar(20)
  ...
}
```

`prisma db push` 불가 — 실행 방법:
```bash
cd /Users/jeonminjun/claude/letmein
PGPASSWORD=letmein psql -h localhost -p 15432 -U letmein -d letmein \
  -c "ALTER TABLE admin_credentials ADD COLUMN IF NOT EXISTS role VARCHAR(20) NOT NULL DEFAULT 'admin';"
cd cms && npx prisma generate --schema=packages/db/prisma/schema.prisma
```

병원 테스트 계정 삽입 (비밀번호: hospital1234):
```bash
HASH=$(node -e "const b=require('bcryptjs');b.hash('hospital1234',10).then(h=>console.log(h))")
PGPASSWORD=letmein psql -h localhost -p 15432 -U letmein -d letmein \
  -c "INSERT INTO admin_credentials (email, password_hash, name, role, is_active) VALUES ('hospital@letmein.kr', '$HASH', '테스트병원', 'hospital', true) ON CONFLICT (email) DO NOTHING;"
# hospitalId와 연결: 먼저 hospitals 테이블에서 id=1 병원의 user_id 확인 후 admin_credentials.id를 user_id처럼 사용할 수 없음
# 주의: AdminCredential.id와 Hospital.userId는 다른 시퀀스 — 별도 매핑 필요
```

**병원 계정-병원 매핑 구조 재고:**

AdminCredential.id와 Hospital.userId는 연결되지 않는다. 이 문제를 해결하기 위해:
- `admin_credentials` 테이블에 `hospital_id BIGINT NULL REFERENCES hospitals(id)` 컬럼 추가
- schema.prisma AdminCredential에도 `hospitalId BigInt? @map("hospital_id")` 추가

```bash
PGPASSWORD=letmein psql -h localhost -p 15432 -U letmein -d letmein \
  -c "ALTER TABLE admin_credentials ADD COLUMN IF NOT EXISTS hospital_id BIGINT REFERENCES hospitals(id);"
```

병원 계정 삽입 (병원 id=1 연결):
```bash
# bcrypt hash 먼저 생성
cd /Users/jeonminjun/claude/letmein/cms
HASH=$(node -e "const b=require('bcryptjs');b.hash('hospital1234',10).then(h=>process.stdout.write(h))")
PGPASSWORD=letmein psql -h localhost -p 15432 -U letmein -d letmein \
  -c "INSERT INTO admin_credentials (email, password_hash, name, role, hospital_id, is_active) VALUES ('hospital@letmein.kr', '$HASH', '테스트병원', 'hospital', 1, true) ON CONFLICT (email) DO NOTHING;"
```

로그인 API에서 hospitalId 조회:
```typescript
// credential.hospitalId가 있으면 그대로 사용
const hospitalId = credential.hospitalId
if (!hospitalId) return 401 "병원 계정에 병원 정보가 연결되어 있지 않습니다"
```

**4. cms/apps/admin/src/lib/session.ts 확장**

기존 `getSessionAdminId()` 유지하고 아래 헬퍼 추가:
```typescript
export async function getSessionRole(): Promise<'admin' | 'hospital' | null>
// JWT 'admin_token' 쿠키에서 payload.role 반환
// 오류 시 null

export async function getSessionHospitalId(): Promise<bigint | null>
// role=hospital 세션에서 payload.hospitalId를 BigInt로 반환
// role이 hospital이 아니거나 hospitalId 없으면 null
```

**5. cms/packages/types/src/letmein.ts 에 HospitalUser 타입 추가**
```typescript
export interface HospitalUser {
  id: number
  email: string
  name: string
  role: 'hospital'
  hospitalId: number
}
```
  </action>
  <verify>
    <automated>
      cd /Users/jeonminjun/claude/letmein/cms && npx tsc --noEmit -p apps/admin/tsconfig.json 2>&1 | head -30
    </automated>
  </verify>
  <done>
    - POST /api/v1/hospital/auth/signin: hospital 계정으로 로그인 성공 시 admin_token 쿠키 설정 + role=hospital payload 포함
    - POST /api/v1/hospital/auth/signout: admin_token 쿠키 삭제
    - admin_credentials 테이블에 role, hospital_id 컬럼 존재
    - getSessionRole(), getSessionHospitalId() 헬퍼 존재
    - HospitalUser 타입이 letmein.ts에 export됨
    - TypeScript 오류 없음
  </done>
</task>

<task type="auto">
  <name>Task 2: 미들웨어 role 분기 + 병원 로그인 페이지 + 병원 레이아웃 (HAUTH-01, HAUTH-02, HAUTH-03)</name>
  <files>
    cms/apps/admin/src/middleware.ts,
    cms/apps/admin/src/app/(hospital)/hospital-login/page.tsx,
    cms/apps/admin/src/app/(hospital)/layout.tsx,
    cms/apps/admin/src/app/(hospital)/(hospital-dashboard)/layout.tsx,
    cms/apps/admin/src/views/hospital/auth/HospitalLoginPage.tsx,
    cms/apps/admin/src/widgets/sidebar/HospitalSidebar.tsx
  </files>
  <action>
**1. cms/apps/admin/src/middleware.ts 수정**

현재 미들웨어는 토큰 유무만 확인. role 분기 로직 추가:

```typescript
import { NextRequest, NextResponse } from 'next/server'
import { jwtVerify } from 'jose'

const SECRET = new TextEncoder().encode(process.env.JWT_SECRET ?? 'beauti-admin-secret-key')

// 어드민 전용 라우트 패턴 (hospital 계정 접근 차단)
const ADMIN_ONLY_PREFIXES = [
  '/coordinator', '/hospitals', '/cast-members', '/cast-stories',
  '/episodes', '/premium', '/ads', '/members', '/operations',
  '/contents', '/config',
]

export async function middleware(request: NextRequest) {
  const { pathname } = request.nextUrl

  // 공개 경로: /login, /hospital-login, /api 통과
  if (
    pathname.startsWith('/login') ||
    pathname.startsWith('/hospital-login') ||
    pathname.startsWith('/api/')
  ) {
    return NextResponse.next()
  }

  const token = request.cookies.get('admin_token')?.value

  if (!token) {
    // 병원 라우트(/hospital/*)는 /hospital-login으로, 나머지는 /login으로
    if (pathname.startsWith('/hospital')) {
      return NextResponse.redirect(new URL('/hospital-login', request.url))
    }
    return NextResponse.redirect(new URL('/login', request.url))
  }

  try {
    const { payload } = await jwtVerify(token, SECRET)
    const role = payload.role as string

    if (role === 'hospital') {
      // hospital 계정이 어드민 전용 라우트에 접근 시 대시보드로 리다이렉트
      const isAdminRoute = ADMIN_ONLY_PREFIXES.some(p => pathname.startsWith(p))
      if (isAdminRoute || pathname === '/') {
        return NextResponse.redirect(new URL('/hospital/dashboard', request.url))
      }
      // /hospital/* 라우트는 통과
      return NextResponse.next()
    } else {
      // admin 계정이 /hospital/* 에 접근 시 어드민 대시보드로 리다이렉트
      if (pathname.startsWith('/hospital')) {
        return NextResponse.redirect(new URL('/', request.url))
      }
      return NextResponse.next()
    }
  } catch {
    const res = pathname.startsWith('/hospital')
      ? NextResponse.redirect(new URL('/hospital-login', request.url))
      : NextResponse.redirect(new URL('/login', request.url))
    res.cookies.delete('admin_token')
    return res
  }
}

export const config = {
  matcher: ['/((?!_next/static|_next/image|favicon.ico).*)'],
}
```

**2. cms/apps/admin/src/views/hospital/auth/HospitalLoginPage.tsx 생성**

`use client` 컴포넌트. 기존 LoginPage(login.tsx)와 동일한 구조 사용:
- react-hook-form + zod: `{ email: string, password: string }`
- POST `/api/v1/hospital/auth/signin`
- 성공 시: `router.replace('/hospital/dashboard')`
- 저장소: Zustand auth store에 `hospital` 계정 저장 (useAuthStore 사용 불가 — AdminUser 타입이 맞지 않음)
  - 별도 Zustand store 생성 대신, 쿠키 기반이므로 로그인 성공 시 router.replace만 해도 충분
- 브랜드: 기존 어드민 로그인과 동일한 레이아웃 유지하되 타이틀을 "병원 포탈 로그인"으로 변경
- 어드민 로그인 링크: 하단에 "관리자 로그인 →" 링크 `/login`

**3. cms/apps/admin/src/app/(hospital)/hospital-login/page.tsx 생성**

```typescript
import { HospitalLoginPage } from '@/views/hospital/auth/HospitalLoginPage'
export default function Page() {
  return <HospitalLoginPage />
}
```

**4. cms/apps/admin/src/app/(hospital)/layout.tsx 생성**

`(hospital)` route group을 위한 최소한의 layout:
```typescript
export default function HospitalAuthLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
```

**5. cms/apps/admin/src/widgets/sidebar/HospitalSidebar.tsx 생성**

`use client` 컴포넌트. 기존 Sidebar(index.tsx)와 동일한 UI 구조이나 병원 전용 메뉴만 포함:

병원 메뉴 항목 (Phase 2~4 기준):
- 대시보드: `/hospital/dashboard` (LayoutDashboard)
- 상담 관리: `/hospital/consultations` (HeartHandshake) — Phase 3용, 현재는 링크만
- 채팅: `/hospital/chat` (MessageSquare) — Phase 4용
- 프리미엄: `/hospital/premium` (Crown) — Phase 4용

로그아웃:
- POST `/api/v1/hospital/auth/signout`
- 성공 시 `router.replace('/hospital-login')`

Props: 없음 (useUIStore, useRouter 직접 사용)

**6. cms/apps/admin/src/app/(hospital)/(hospital-dashboard)/layout.tsx 생성**

병원 대시보드 레이아웃 — HospitalSidebar 포함:
```typescript
import { HospitalSidebar } from '@/widgets/sidebar/HospitalSidebar'
import { Header } from '@/widgets/header'

export default function HospitalDashboardLayout({ children }: { children: React.ReactNode }) {
  return (
    <div className="flex min-h-screen">
      <HospitalSidebar />
      <div className="flex flex-1 flex-col min-w-0">
        <Header />
        <main className="flex-1 overflow-x-auto p-6">{children}</main>
      </div>
    </div>
  )
}
```

**라우트 구조 결과:**
```
/hospital-login         → (hospital)/hospital-login/page.tsx
/hospital/dashboard     → (hospital)/(hospital-dashboard)/page.tsx (Task 3에서 생성)
```
  </action>
  <verify>
    <automated>
      cd /Users/jeonminjun/claude/letmein/cms && npx tsc --noEmit -p apps/admin/tsconfig.json 2>&1 | head -30
    </automated>
  </verify>
  <done>
    - /hospital-login 경로에 페이지 파일 존재
    - middleware.ts에 role 분기 로직 존재 (hospital → 어드민 라우트 차단, admin → /hospital/* 차단)
    - HospitalSidebar.tsx에 병원 메뉴 + 로그아웃 버튼 존재
    - (hospital)/(hospital-dashboard)/layout.tsx에 HospitalSidebar import 존재
    - TypeScript 오류 없음
  </done>
</task>

<task type="auto">
  <name>Task 3: 병원 대시보드 API + UI (HDASH-01, HDASH-02, HDASH-03)</name>
  <files>
    cms/apps/admin/src/app/api/v1/hospital/dashboard/route.ts,
    cms/apps/admin/src/app/(hospital)/(hospital-dashboard)/page.tsx,
    cms/apps/admin/src/views/hospital/dashboard/HospitalDashboardView.tsx
  </files>
  <action>
**1. cms/apps/admin/src/app/api/v1/hospital/dashboard/route.ts 생성**

GET handler — 인증 확인 후 병원 대시보드 통계 반환:

```typescript
import { NextResponse } from 'next/server'
import { prisma } from '@letmein/db'
import { getSessionHospitalId } from '@/lib/session'

export async function GET() {
  const hospitalId = await getSessionHospitalId()
  if (!hospitalId) {
    return NextResponse.json({ success: false, error: { code: 'UNAUTHORIZED' } }, { status: 401 })
  }

  const [newMatches, activeChats, hospital, recentConsultations] = await Promise.all([
    // HDASH-01: 신규 매칭 수 — 최근 30일 내 matched 상태 CoordinatorMatch
    prisma.coordinatorMatch.count({
      where: {
        hospitalId,
        createdAt: { gte: new Date(Date.now() - 30 * 24 * 60 * 60 * 1000) },
      },
    }),

    // HDASH-01: 활성 채팅 수 — status=active인 ChatRoom
    prisma.chatRoom.count({
      where: { hospitalId, status: 'active' },
    }),

    // HDASH-01: 평균 응답률 + 평점 — Hospital 레코드에서 avgRating 조회
    // 응답률 = consultation_responses 수 / CoordinatorMatch 수 (전체)
    prisma.hospital.findUnique({
      where: { id: hospitalId },
      select: { avgRating: true },
    }),

    // HDASH-02: 최근 상담 요청 3건 — hospitalId에 매칭된 CoordinatorMatch의 request
    prisma.coordinatorMatch.findMany({
      where: { hospitalId },
      orderBy: { createdAt: 'desc' },
      take: 3,
      select: {
        id: true,
        createdAt: true,
        status: true,
        request: {
          select: {
            id: true,
            description: true,
            status: true,
            createdAt: true,
            procedure_categories: { select: { name: true } },
          },
        },
      },
    }),
  ])

  // 응답률 계산
  const totalMatches = await prisma.coordinatorMatch.count({ where: { hospitalId } })
  const totalResponses = await prisma.consultation_responses.count({
    where: { hospital_id: hospitalId },
  })
  const responseRate = totalMatches > 0
    ? Math.round((totalResponses / totalMatches) * 100)
    : 0

  // HDASH-03: 최근 리뷰 3건 — reviews 테이블은 Prisma 스키마 없음, $queryRaw 사용
  const recentReviews = await prisma.$queryRaw<Array<{
    id: bigint
    rating: number
    content: string
    created_at: Date
  }>>`
    SELECT id, rating, content, created_at
    FROM reviews
    WHERE hospital_id = ${hospitalId} AND status = 'active'
    ORDER BY created_at DESC
    LIMIT 3
  `

  return NextResponse.json({
    success: true,
    data: {
      stats: {
        newMatches,
        activeChats,
        responseRate,
        avgRating: hospital?.avgRating ? Number(hospital.avgRating) : 0,
      },
      recentConsultations: recentConsultations.map(m => ({
        id: m.id.toString(),
        requestId: m.request.id.toString(),
        category: m.request.procedure_categories.name,
        description: m.request.description.slice(0, 60),
        status: m.request.status,
        matchedAt: m.createdAt.toISOString(),
      })),
      recentReviews: recentReviews.map(r => ({
        id: r.id.toString(),
        rating: r.rating,
        content: r.content.slice(0, 80),
        createdAt: r.created_at.toISOString(),
      })),
    },
  })
}
```

주의사항:
- `prisma.consultation_responses`는 모델명 그대로 사용 (snake_case)
- BigInt 직렬화: `.toString()` 처리
- avgRating: Prisma가 `Decimal` 반환 → `Number()` 변환

**2. cms/apps/admin/src/views/hospital/dashboard/HospitalDashboardView.tsx 생성**

`use client` 컴포넌트. React Query 사용하지 않고 props로 데이터 받음 (page.tsx에서 fetch):

Props 타입:
```typescript
interface DashboardStats {
  newMatches: number
  activeChats: number
  responseRate: number
  avgRating: number
}
interface RecentConsultation {
  id: string
  requestId: string
  category: string
  description: string
  status: string
  matchedAt: string
}
interface RecentReview {
  id: string
  rating: number
  content: string
  createdAt: string
}
interface HospitalDashboardViewProps {
  stats: DashboardStats
  recentConsultations: RecentConsultation[]
  recentReviews: RecentReview[]
}
```

UI 구조:
```
페이지 제목: "대시보드"

[통계 카드 4개 — 2열 그리드 (md:4열)]
┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐
│ 신규 매칭   │ │ 활성 채팅   │ │ 평균 응답률 │ │ 평점        │
│ {N}건       │ │ {N}건       │ │ {N}%        │ │ ★ {N.N}    │
└─────────────┘ └─────────────┘ └─────────────┘ └─────────────┘

[최근 상담 요청] — 드롭다운/아코디언 금지, 항상 펼쳐서 표시
섹션 제목 + "전체 보기 →" 링크

[카드 리스트 — 3건]
┌──────────────────────────────────────────┐
│ 카테고리 칩  |  상태 칩  |  날짜         │
│ 설명 요약(60자)                          │
└──────────────────────────────────────────┘

[최근 리뷰] — 항상 펼쳐서 표시
섹션 제목

[카드 리스트 — 3건]
┌──────────────────────────────────────────┐
│ ★★★★★  |  날짜                          │
│ 내용(80자 요약)                           │
└──────────────────────────────────────────┘
```

UI 규칙 (CLAUDE.md 준수):
- Accordion/Collapsible 절대 사용 금지
- 드롭다운 사용 금지
- 탭/칩만 사용

**3. cms/apps/admin/src/app/(hospital)/(hospital-dashboard)/page.tsx 생성**

Server Component — dashboard API를 server-side에서 호출:

```typescript
import { cookies } from 'next/headers'
import { redirect } from 'next/navigation'
import { HospitalDashboardView } from '@/views/hospital/dashboard/HospitalDashboardView'
import { getSessionHospitalId } from '@/lib/session'

export default async function Page() {
  const hospitalId = await getSessionHospitalId()
  if (!hospitalId) redirect('/hospital-login')

  // 내부 fetch 대신 Prisma 직접 호출 (same process)
  // dashboard route handler 로직을 inline으로 실행
  const { prisma } = await import('@letmein/db')

  const [newMatches, activeChats, hospital, recentConsultations] = await Promise.all([
    prisma.coordinatorMatch.count({
      where: {
        hospitalId,
        createdAt: { gte: new Date(Date.now() - 30 * 24 * 60 * 60 * 1000) },
      },
    }),
    prisma.chatRoom.count({ where: { hospitalId, status: 'active' } }),
    prisma.hospital.findUnique({
      where: { id: hospitalId },
      select: { avgRating: true },
    }),
    prisma.coordinatorMatch.findMany({
      where: { hospitalId },
      orderBy: { createdAt: 'desc' },
      take: 3,
      select: {
        id: true,
        createdAt: true,
        status: true,
        request: {
          select: {
            id: true,
            description: true,
            status: true,
            createdAt: true,
            procedure_categories: { select: { name: true } },
          },
        },
      },
    }),
  ])

  const totalMatches = await prisma.coordinatorMatch.count({ where: { hospitalId } })
  const totalResponses = await prisma.consultation_responses.count({
    where: { hospital_id: hospitalId },
  })
  const responseRate = totalMatches > 0
    ? Math.round((totalResponses / totalMatches) * 100)
    : 0

  const recentReviewsRaw = await prisma.$queryRaw<Array<{
    id: bigint; rating: number; content: string; created_at: Date
  }>>`
    SELECT id, rating, content, created_at
    FROM reviews
    WHERE hospital_id = ${hospitalId} AND status = 'active'
    ORDER BY created_at DESC LIMIT 3
  `

  const stats = {
    newMatches,
    activeChats,
    responseRate,
    avgRating: hospital?.avgRating ? Number(hospital.avgRating) : 0,
  }

  const recentConsultationsData = recentConsultations.map(m => ({
    id: m.id.toString(),
    requestId: m.request.id.toString(),
    category: m.request.procedure_categories.name,
    description: m.request.description.slice(0, 60),
    status: m.request.status ?? 'active',
    matchedAt: m.createdAt.toISOString(),
  }))

  const recentReviews = recentReviewsRaw.map(r => ({
    id: r.id.toString(),
    rating: r.rating,
    content: r.content.slice(0, 80),
    createdAt: r.created_at.toISOString(),
  }))

  return (
    <HospitalDashboardView
      stats={stats}
      recentConsultations={recentConsultationsData}
      recentReviews={recentReviews}
    />
  )
}
```
  </action>
  <verify>
    <automated>
      cd /Users/jeonminjun/claude/letmein/cms && npx tsc --noEmit -p apps/admin/tsconfig.json 2>&1 | head -50
    </automated>
  </verify>
  <done>
    - /hospital/dashboard 경로에 page.tsx 존재
    - HospitalDashboardView에 stats(4개), recentConsultations(3건), recentReviews(3건) 렌더링
    - dashboard API route에 hospitalId 기반 Prisma 조회 로직 존재
    - BigInt 직렬화 처리됨 (toString() 호출)
    - reviews 테이블은 prisma.$queryRaw로 조회
    - TypeScript 오류 없음 (수정된 파일 기준)
  </done>
</task>

</tasks>

<verification>
모든 태스크 완료 후 다음을 확인한다:

1. **라우트 존재 확인:**
   ```bash
   ls /Users/jeonminjun/claude/letmein/cms/apps/admin/src/app/\(hospital\)/hospital-login/
   ls /Users/jeonminjun/claude/letmein/cms/apps/admin/src/app/\(hospital\)/\(hospital-dashboard\)/
   ```

2. **미들웨어 role 분기 존재:**
   ```bash
   grep -n "hospital" /Users/jeonminjun/claude/letmein/cms/apps/admin/src/middleware.ts
   ```

3. **DB 컬럼 확인:**
   ```bash
   PGPASSWORD=letmein psql -h localhost -p 15432 -U letmein -d letmein \
     -c "\d admin_credentials"
   ```

4. **TypeScript 컴파일:**
   ```bash
   cd /Users/jeonminjun/claude/letmein/cms && npx tsc --noEmit -p apps/admin/tsconfig.json 2>&1 | head -20
   ```

5. **Phase 2 성공 기준 체크:**
   - [ ] /hospital-login 페이지 파일 존재
   - [ ] 미들웨어: hospital 계정 → 어드민 라우트 차단, admin 계정 → /hospital/* 차단
   - [ ] HospitalSidebar.tsx 존재
   - [ ] (hospital)/(hospital-dashboard)/layout.tsx에 HospitalSidebar 사용
   - [ ] 대시보드 page.tsx: stats(newMatches, activeChats, responseRate, avgRating) 조회
   - [ ] 대시보드 page.tsx: recentConsultations 3건 조회
   - [ ] 대시보드 page.tsx: recentReviews $queryRaw 조회
</verification>

<success_criteria>
- 병원 계정으로 /hospital-login에서 로그인하면 hospital 전용 사이드바와 대시보드가 표시된다
- 같은 CMS 앱에서 admin 계정은 어드민 사이드바, hospital 계정은 병원 사이드바를 각각 표시한다
- 병원 계정이 /coordinator, /hospitals 등 어드민 전용 라우트에 접근하면 /hospital/dashboard로 리다이렉트된다
- 병원 대시보드에서 신규 매칭 수, 활성 채팅 수, 평균 응답률, 평점 4개 통계가 표시된다
- 대시보드에서 최근 상담 요청 3건 미리보기가 표시된다
- 대시보드에서 최근 리뷰 3건 미리보기가 표시된다
</success_criteria>

<output>
완료 후 `.planning/phases/02-hospital-auth-dashboard/02-01-SUMMARY.md` 생성.
</output>
