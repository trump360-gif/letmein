{
  "name": "@letmein/utils",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "clsx": "latest",
    "tailwind-merge": "latest",
    "date-fns": "^3.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
